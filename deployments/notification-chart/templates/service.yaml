apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: backend
spec:
  selector:
    app: notification
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: {{ .Values.service.type }}
