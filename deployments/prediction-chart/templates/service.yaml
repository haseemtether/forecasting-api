apiVersion: v1
kind: Service
metadata:
  name: prediction-service
  namespace: backend
spec:
  selector:
    app: prediction
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: {{ .Values.service.type }}
