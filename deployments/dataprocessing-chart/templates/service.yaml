apiVersion: v1
kind: Service
metadata:
  name: data-processing-service
  namespace: backend
spec:
  selector:
    app: data-processing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001
  type: {{ .Values.service.type }}
